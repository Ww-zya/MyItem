server:
  port: 9090
spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/qwd_community?characterEncoding=UTF-8
    username: root
    password:

#配置mapper xml文件的路径
mybatis:
  mapper-locations: classpath*:/mapper/*.xml

